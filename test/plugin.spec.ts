import "mocha";
import { assert } from "chai";
import { Test } from "mocha";

describe("Test Suite - massa React wallet plugin", () => {
	describe("Test case - Massa React Wallet Plugin", () => {

		it("Inject.", () => {
			assert(true);
		});

		it("Unmount.", async () => {
			assert(true);
		});
	});
});
